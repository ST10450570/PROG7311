version: '3.8'

services:
  glms-api:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
    # Optional: mount source volume for hot reload (if using `dotnet watch`)
    # volumes:
    #   - ./GLMS.API:/src
    # working_dir: /src
    # command: dotnet watch run --no-launch-profile

  glms-web:
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      # Override API base URL for local development (when running outside full compose)
      # ApiSettings__BaseUrl: https://localhost:7100
    # Optional: mount source volume for hot reload
    # volumes:
    #   - ./GLMS.Web:/src
    # working_dir: /src
    # command: dotnet watch run --no-launch-profile